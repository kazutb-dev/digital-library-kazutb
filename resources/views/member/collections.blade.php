@extends('layouts.member', ['title' => __('librarian.member.collections.title')])
@section('content')
@include('member.partials.flash')
<div class="space-y-10"><header><h1 class="font-headline text-4xl">{{ __('librarian.member.collections.title') }}</h1><p class="mt-2 text-slate-600">{{ __('librarian.member.collections.subtitle') }}</p></header>
<form method="POST" action="{{ route('member.collections.store') }}" class="grid gap-3 rounded-2xl bg-white p-5 md:grid-cols-[1fr_2fr_auto]">@csrf<input required maxlength="180" class="rounded-lg border-slate-300" name="title" placeholder="{{ __('librarian.member.collections.name') }}"><input maxlength="3000" class="rounded-lg border-slate-300" name="description" placeholder="{{ __('librarian.member.collections.description') }}"><button class="rounded-lg bg-primary px-5 py-2 text-white">{{ __('librarian.member.collections.create') }}</button></form>
<section><h2 class="mb-4 font-headline text-2xl">{{ __('librarian.member.collections.mine') }}</h2><div class="grid gap-4 md:grid-cols-3">@forelse($ownCollections as $collection)<a class="rounded-2xl bg-white p-5 hover:ring-2 hover:ring-secondary" href="{{ route('member.collections.show',$collection) }}"><span class="text-xs uppercase text-secondary">{{ __('librarian.member.collections.types.'.$collection->collection_type) }}</span><h3 class="mt-2 font-headline text-xl">{{ $collection->title }}</h3><p class="mt-3 text-sm text-slate-500">{{ __('librarian.member.collections.books_count',['count'=>$collection->items_count]) }}</p></a>@empty<p class="text-slate-500">{{ __('librarian.member.collections.empty') }}</p>@endforelse</div></section>
<section><h2 class="mb-4 font-headline text-2xl">{{ __('librarian.member.collections.public') }}</h2><div class="grid gap-4 md:grid-cols-3">@forelse($publicCollections as $collection)<a class="rounded-2xl bg-white p-5" href="{{ route('member.collections.show',$collection) }}"><span class="text-xs uppercase text-secondary">{{ __('librarian.member.collections.types.'.$collection->collection_type) }}</span><h3 class="mt-2 font-headline text-xl">{{ $collection->title }}</h3><p class="mt-3 text-sm text-slate-500">{{ __('librarian.member.collections.books_count',['count'=>$collection->items_count]) }}</p></a>@empty<p class="text-slate-500">{{ __('librarian.member.collections.public_empty') }}</p>@endforelse</div></section></div>
@endsection
