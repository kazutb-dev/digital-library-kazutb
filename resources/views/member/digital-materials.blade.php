@extends('layouts.member', ['title' => __('librarian.member.digital.title')])
@section('content')
<div class="space-y-8"><header><h1 class="font-headline text-4xl text-primary">{{ __('librarian.member.digital.title') }}</h1><p class="mt-2 text-slate-600">{{ __('librarian.member.digital.subtitle') }}</p></header>
@if($materials->isEmpty())<div class="rounded-2xl bg-white p-10 text-center text-slate-500">{{ __('librarian.member.digital.empty') }}</div>@else
<div class="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">@foreach($materials as $entry) @php($material=$entry['model'])<article class="rounded-2xl bg-white p-5"><span class="text-xs font-bold uppercase text-secondary">{{ __('digital.file_types.'.$material->file_type) }} · {{ __('digital.access.'.$material->access_level) }}</span><h2 class="mt-2 font-headline text-xl">{{ $material->title }}</h2><p class="mt-1 text-sm text-slate-600">{{ $material->bibliographicRecord?->primary_author ?: '—' }}</p><p class="mt-3 text-xs text-slate-500">{{ $material->license_terms ?: __('librarian.member.digital.license_default') }}</p><a class="mt-5 inline-flex rounded-lg bg-primary px-4 py-2 text-sm text-white" href="{{ $entry['payload']['viewerUrl'] ?? '/digital-viewer/'.$material->id }}">{{ __('librarian.member.digital.read') }}</a>@if($entry['payload']['downloadUrl'] ?? null)<a class="ml-2 text-sm text-secondary" href="{{ $entry['payload']['downloadUrl'] }}">{{ __('librarian.member.digital.download') }}</a>@endif</article>@endforeach</div><div>{{ $materials->links() }}</div>@endif</div>
@endsection
